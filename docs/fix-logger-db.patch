diff --git a/app/db.py b/app/db.py
index baa4584..ca7e30e 100644
--- a/app/db.py
+++ b/app/db.py
@@ -10,7 +10,7 @@ DEFAULT_SETTINGS = {
     'check_interval_normal': '240',
     'check_interval_failover': '30',
     'tcp_timeout': '3',
-    'curl_timeout': '10',
+    'curl_timeout': '5',
     'test_url': 'http://www.gstatic.com/generate_204',
     'auto_failover': 'true',
     'web_port': '8080',
diff --git a/app/logger.py b/app/logger.py
index 325ad65..69cc162 100644
--- a/app/logger.py
+++ b/app/logger.py
@@ -62,8 +62,12 @@ class LogWriter:
 
     def write(self, text):
         """写入文本"""
-        # 输出到原始流
-        self.original.write(text)
+        # 输出到原始流（处理 Unicode）
+        try:
+            self.original.write(text)
+        except UnicodeEncodeError:
+            # 替换无法编码的字符
+            self.original.write(text.encode('utf-8', errors='replace').decode('utf-8'))
 
         # 如果有内容，添加到日志
         text = text.strip()
